<h1>Bas lieu</h1>

<div class="tassevent">
<div class="sectorJob">
    <div class="sectorBox Agriculture"><%= link_to 'Agriculture et pêche', secteurs_baslieu_agriculture_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Agriculture et pêche', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>
    </div>

    <div class="sectorBox espacevert"><%= link_to 'Espaces verts', secteurs_baslieu_espacevert_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Espaces verts', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>
</div>

        <div class="sectorBox animals"><%= link_to 'Soins aux animaux', secteurs_baslieu_animals_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Soins aux animaux', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>
    </div>
     
        <div class="sectorBox art"><%= link_to 'Ouvrage d\'art', secteurs_baslieu_art_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Ouvrage d\'art', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>  
    </div>
</div>

<div class="sectorJob">
          <div class="sectorBox banque"><%= link_to 'Banque, assur. immo.', secteurs_baslieu_banque_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Banque, assurance, immobilier', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>  
     </div>

      <div class="sectorBox commerce"><%= link_to 'Commerce, Vente', secteurs_baslieu_commerce_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Commerce, Vente', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
   </div>
          
        <div class="sectorBox communication"><%= link_to 'Communication', secteurs_baslieu_communication_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Communication', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>

        <div class="sectorBox batiment"><%= link_to 'Bâtiment/Travaux public', secteurs_baslieu_batiment_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Bâtiment', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div> 

<div class="sectorJob">
        <div class="sectorBox restauration"><%= link_to 'Hôtellerie/Restauration', secteurs_baslieu_restauration_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Restauration', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>  
    </div>

        <div class="sectorBox tourisme"><%= link_to 'Tourisme, Loisirs', secteurs_baslieu_tourisme_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Tourisme, Loisirs', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>   
    </div>
          
      <div class="sectorBox industrieagroalimentaire"><%= link_to 'Industrie Agroalimentaire', secteurs_baslieu_industrieagroalimentaire_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Industrie Agroalimentaire', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>   
    </div>

      <div class="sectorBox industriemetallurgie"><%= link_to 'Industrie Metallurgie', secteurs_baslieu_industriemetallurgie_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Industrie Metallurgie', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>   
    </div>
</div> 

<div class="sectorJob">
      <div class="sectorBox industrieautomobile"><%= link_to 'Industrie Automobile', secteurs_baslieu_industrieautomobile_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Industrie Automobile', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>   
    </div>

       <div class="sectorBox maintenance"><%= link_to 'Maintenance', secteurs_baslieu_maintenance_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Maintenance', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>

        <div class="sectorBox sante"><%= link_to 'Santé', secteurs_baslieu_sante_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Santé', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>

    <div class="sectorBox service"><%= link_to 'Service à la personne', secteurs_baslieu_service_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Service à la personne', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div> 

<div class="sectorJob">  
        <div class="sectorBox spectacle"><%= link_to 'Spectacle/Événementiel', secteurs_baslieu_spectacle_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Spectacle', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>  
    </div>

       <div class="sectorBox support"><%= link_to 'Support à l\'entreprise', secteurs_baslieu_support_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Support à l\'entreprise', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>  
    </div>

        <div class="sectorBox transport"><%= link_to 'Transport, Logistique', secteurs_baslieu_transport_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Transport, Logistique', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %>
    </div>

        <div class="sectorBox collectivite"><%= link_to 'Non marchant', secteurs_baslieu_nonmarchant_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Non marchant', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div>

<div class="sectorJob">   
       <div class="sectorBox association"><%= link_to 'Association', secteurs_baslieu_association_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Association', commune: 'Bas lieu').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div>    
</div>  