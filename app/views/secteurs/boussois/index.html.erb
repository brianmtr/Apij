<h1>Boussois</h1>

<div class="test">
<div class="sectorJob">
    <div class="sectorBox Agriculture"><%= link_to 'Agriculture et pêche', secteurs_boussois_agriculture_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Agriculture et pêche', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>
    </div>

    <div class="sectorBox espacevert"><%= link_to 'Espaces verts', secteurs_boussois_espacevert_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Espaces verts', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>
    </div>

        <div class="sectorBox animals"><%= link_to 'Soins aux animaux', secteurs_boussois_animals_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Soins aux animaux', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>
    </div>
            
        <div class="sectorBox art"><%= link_to 'Ouvrage d\'art', secteurs_boussois_art_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Ouvrage d\'art', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>  
    </div>
</div>

<div class="sectorJob">
          <div class="sectorBox banque"><%= link_to 'Banque, assur. immo.', secteurs_boussois_banque_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Banque, assurance, immobilier', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>  
     </div>

      <div class="sectorBox commerce"><%= link_to 'Commerce, Vente', secteurs_boussois_commerce_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Commerce, Vente', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
   </div>
          
        <div class="sectorBox communication"><%= link_to 'Communication', secteurs_boussois_communication_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Communication', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>

        <div class="sectorBox batiment"><%= link_to 'Bâtiment/Travaux public', secteurs_boussois_batiment_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Bâtiment', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div>

<div class="sectorJob">
        <div class="sectorBox restauration"><%= link_to 'Hôtellerie/Restauration', secteurs_boussois_restauration_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Restauration', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>  
    </div>

        <div class="sectorBox tourisme"><%= link_to 'Tourisme, Loisirs', secteurs_boussois_tourisme_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Tourisme, Loisirs', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>   
    </div>
          
      <div class="sectorBox industrieagroalimentaire"><%= link_to 'Industrie Agroalimentaire', secteurs_boussois_industrieagroalimentaire_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Industrie Agroalimentaire', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>   
    </div>

      <div class="sectorBox industriemetallurgie"><%= link_to 'Industrie Metallurgie', secteurs_boussois_industriemetallurgie_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Industrie Metallurgie', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>   
    </div>
</div> 

<div class="sectorJob">
      <div class="sectorBox industrieautomobile"><%= link_to 'Industrie Automobile', secteurs_boussois_industrieautomobile_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Industrie Automobile', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>   
    </div>

       <div class="sectorBox maintenance"><%= link_to 'Maintenance', secteurs_boussois_maintenance_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Maintenance', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>

        <div class="sectorBox sante"><%= link_to 'Santé', secteurs_boussois_sante_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Santé', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>

    <div class="sectorBox service"><%= link_to 'Service à la personne', secteurs_boussois_service_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Service à la personne', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div>

<div class="sectorJob">  
        <div class="sectorBox spectacle"><%= link_to 'Spectacle/Événementiel', secteurs_boussois_spectacle_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Spectacle', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>  
    </div>
  
       <div class="sectorBox support"><%= link_to 'Support à l\'entreprise', secteurs_boussois_support_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Support à l\'entreprise', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>  
    </div>

        <div class="sectorBox transport"><%= link_to 'Transport, Logistique', secteurs_boussois_transport_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Transport, Logistique', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %>
    </div>

        <div class="sectorBox collectivite"><%= link_to 'Non marchant', secteurs_boussois_nonmarchant_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Non marchant', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div>

<div class="sectorJob">   
       <div class="sectorBox association"><%= link_to 'Association', secteurs_boussois_association_index_path %>
        <% @offres.each do |offre| %>
            <div class="containerBadge">
                <div class="badge"><p><%= @offres.where(domaine: 'Association', commune: 'Boussois').count(1) %><p></div>
            </div>
        <% end %> 
    </div>
</div>    
</div>